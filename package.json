{
  "name": "my-app",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --packages external --outfile bundle.main.js --minify --sourcemap",
    "start": "bun run --smol bundle.main.js"
  },
  "dependencies": {
    "hono": "^4.6.9",
    "pug": "^3.0.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/pug": "^2.0.10"
  }
}